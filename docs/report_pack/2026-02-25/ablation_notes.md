# Ablation Notes

## baseline vs zero_velocity (T0)
- Dataset: `/root/projects/4d-recon/data/selfcap_bar_8cam60f`
- Key settings: `default_keyframe_small`, `max-steps=200`, `render-traj-path=fixed`, `global-scale=12`, `t0-debug-interval=50`
- Outcome: PASS (`baseline` and `zero_velocity` both finite and non-zero gradient signals)

## Gate-0 real smoke (4DGV_DeskGames)
- Dataset:
- Key settings:
- Outcome:
