# Plan‑B 执行文档：triangulation → 3D velocity init（48h timebox）

日期：2026-02-26  
主阵地：`/root/projects/4d-recon`  
拍板来源：`docs/decisions/2026-02-26-planb-pivot.md`、`docs/reviews/2026-02-26/meeting-decision.md`  
协议：`protocol_v1`（`docs/protocol.yaml`）不改

> 目的：在 **48h** 内给出 Plan‑B 的 Go/No‑Go；不允许拖成“新主线无限探索”。

---

## 0. 不可违反的纪律

1. 不改 `data/`、不改 `docs/protocols/protocol_v1.yaml`。
2. Plan‑B 只输出到 `outputs/plan_b/...`（init 与统计），训练结果输出到 `outputs/protocol_v1/...`（便于 scoreboard/report-pack）。
3. **Day2 只允许 1 次 full600**：`planb_init_600`。

---

## 1. Gate‑B1（Day1）：生成 init + smoke200 sanity（不许跳）

### 1.1 预检

```bash
cd /root/projects/4d-recon
PY=/root/projects/4d-recon/third_party/FreeTimeGsVanilla/.venv/bin/python
python3 scripts/tests/test_pack_evidence.py
python3 scripts/tests/test_build_report_pack.py
python3 scripts/tests/test_summarize_scoreboard.py
test -d data/selfcap_bar_8cam60f/triangulation
```

### 1.2 生成 baseline init（作为模板，不改变训练代码）

> 说明：Plan‑B 只替换 velocities；positions/colors/times/durations 复用 baseline init，避免 schema/单位翻车。

优先复用 canonical baseline 的 init（如果存在）：
- `outputs/protocol_v1/selfcap_bar_8cam60f/baseline_600/keyframes_60frames_step5.npz`

若本机缺失该文件，则只跑 **CPU 预处理**生成一份模板（不训练）：

```bash
$PY third_party/FreeTimeGsVanilla/src/combine_frames_fast_keyframes.py \
  --input-dir data/selfcap_bar_8cam60f/triangulation \
  --output-path outputs/plan_b/selfcap_bar_8cam60f/_baseline_init/keyframes_60frames_step5.npz \
  --frame-start 0 \
  --frame-end 59 \
  --keyframe-step 5
```

### 1.3 运行 Plan‑B 初始化脚本（输出隔离 + 必须自检落盘）

```bash
$PY scripts/init_velocity_from_points.py \
  --data_dir data/selfcap_bar_8cam60f \
  --baseline_init_npz outputs/protocol_v1/selfcap_bar_8cam60f/baseline_600/keyframes_60frames_step5.npz \
  --frame_start 0 \
  --frame_end_exclusive 60 \
  --keyframe_step 5 \
  --out_dir outputs/plan_b/selfcap_bar_8cam60f
```

必须生成：
- `outputs/plan_b/selfcap_bar_8cam60f/init_points_planb_step5.npz`
- `outputs/plan_b/selfcap_bar_8cam60f/velocity_stats.json`

### 1.4 smoke200 sanity：baseline_init vs planb_init

1) baseline_init smoke200（对照）

```bash
GPU=0 MAX_STEPS=200 \
RESULT_DIR=outputs/protocol_v1/selfcap_bar_8cam60f/baseline_smoke200_planb_window \
bash scripts/run_train_baseline_selfcap.sh
```

2) planb_init smoke200（只变 init）

```bash
GPU=0 MAX_STEPS=200 \
PLANB_INIT_NPZ=outputs/plan_b/selfcap_bar_8cam60f/init_points_planb_step5.npz \
RESULT_DIR=outputs/protocol_v1/selfcap_bar_8cam60f/planb_init_smoke200 \
bash scripts/run_train_planb_init_selfcap.sh
```

Gate‑B1 直接 No‑Go 条件（任一触发即停）：
- 训练不稳定（loss 爆/渲染发散）
- `velocity_stats.json` 显示匹配率极低、或 `||v||` 分布离谱（p99/max 极大）
- smoke200 全线变差且肉眼更糟

---

## 2. Gate‑B2（Day2）：仅 1 次 full600（planb_init_600）

```bash
GPU=0 MAX_STEPS=600 \
PLANB_INIT_NPZ=outputs/plan_b/selfcap_bar_8cam60f/init_points_planb_step5.npz \
RESULT_DIR=outputs/protocol_v1/selfcap_bar_8cam60f/planb_init_600 \
bash scripts/run_train_planb_init_selfcap.sh
```

跑完立刻按决议口径判断 Go/No‑Go：
- Go：`tLPIPS -5%`（vs `baseline_600`）且 PSNR 不劣化 >0.2dB；或 ghosting 明显减少且训练稳定
- No‑Go：三项全劣化或训练明显不稳

---

## 3. Day3 防守：seg200_260 baseline vs control（anti‑cherrypick）

> 注意：seg2 是附录协议证据位，不替代 canonical `protocol_v1`。

数据目录（应已存在）：
- `data/selfcap_bar_8cam60f_seg200_260`

### 3.1 baseline（seg2）

```bash
GPU=0 MAX_STEPS=600 \
DATA_DIR=data/selfcap_bar_8cam60f_seg200_260 \
RESULT_DIR=outputs/protocol_v1_seg200_260/selfcap_bar_8cam60f_seg200_260/baseline_600 \
bash scripts/run_train_baseline_selfcap.sh
```

### 3.2 control_weak_nocue（seg2）

```bash
GPU=0 MAX_STEPS=600 \
DATA_DIR=data/selfcap_bar_8cam60f_seg200_260 \
CUE_TAG=selfcap_bar_8cam60f_seg200_260_zeros_control \
RESULT_DIR=outputs/protocol_v1_seg200_260/selfcap_bar_8cam60f_seg200_260/control_weak_nocue_600 \
bash scripts/run_train_control_weak_nocue_selfcap.sh
```

---

## 4. 报表与证据链刷新（每次 full600 后必做）

```bash
python3 scripts/build_report_pack.py
python3 scripts/summarize_scoreboard.py
python3 scripts/pack_evidence.py
```

---

## 5. 产物路径规范（用于团队对齐）

1. Plan‑B init 与自检（禁止写入 `data/`）：
- `outputs/plan_b/selfcap_bar_8cam60f/init_points_planb_step5.npz`
- `outputs/plan_b/selfcap_bar_8cam60f/velocity_stats.json`

2. 训练结果（进入 report-pack/scoreboard 的入口）：
- `outputs/protocol_v1/selfcap_bar_8cam60f/planb_init_smoke200/`
- `outputs/protocol_v1/selfcap_bar_8cam60f/planb_init_600/`
