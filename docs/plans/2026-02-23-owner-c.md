# Owner C (GPU2) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在 `projects/4d-recon`（主阵地）产出“可汇报的证据包 + 可复现脚本”，并用 GPU2 完成一轮 `baseline vs zero-velocity` 的 T0 审计（优先 SelfCap 真动态；若 B 未完成则用现成 fixture 兜底）。

**Architecture:** 证据包统一落地到 `outputs/report_pack/`，由脚本自动汇总 `outputs/**/stats/val_step*.json` 并产出 `metrics.csv`。T0 审计不依赖 `run_pipeline.sh`（避免被 30000 steps 卡死），直接调用 `combine_frames_fast_keyframes.py` 生成 NPZ，再分别调用 trainer 两次（baseline / `--force-zero-velocity-for-t0`）并输出 `t0_grad.csv` 与视频。

**Tech Stack:** Bash, Python, NumPy, PyTorch, FreeTimeGsVanilla

---

## 约束（避免和 A/B 冲突）

- 不改 `README.md`、不改 `third_party/FreeTimeGsVanilla/run_pipeline.sh`（A 在同步盖章）。
- 不改 `scripts/adapt_selfcap_release_to_freetime.py`（B 在实现）。
- 只新增“证据包/复现/审计”相关脚本与文档。

---

### Task C1: 证据包骨架 + 自动汇总脚本

**Files:**
- Create: `projects/4d-recon/scripts/build_report_pack.py`
- Create: `projects/4d-recon/scripts/run_mvp_repro.sh`
- Create: `projects/4d-recon/outputs/report_pack/metrics.csv`
- Create: `projects/4d-recon/outputs/report_pack/ablation_notes.md`
- Create: `projects/4d-recon/outputs/report_pack/failure_cases.md`
- Create: `projects/4d-recon/notes/demo-runbook.md`
- Create: `projects/4d-recon/notes/qna.md`

**Step 1: 创建目录**

Run:
```bash
cd /root/projects/4d-recon
mkdir -p outputs/report_pack
```

Expected:
- `test -d outputs/report_pack` 返回码为 0。

**Step 2: 写汇总脚本 `scripts/build_report_pack.py`（只依赖 stdlib）**

要求：
- 扫描：`outputs/**/stats/val_step*.json`
- 每个 run 取最大 step 的 `val_stepXXXX.json`
- 输出：`outputs/report_pack/metrics.csv`
  - 列：`run_dir,step,psnr,ssim,lpips,num_gs,notes`

Run:
```bash
cd /root/projects/4d-recon
python3 scripts/build_report_pack.py
```

Expected:
- 若当前还没 run：脚本也应生成空表头 `metrics.csv`（不报错）。

**Step 3: 写一键复现脚本 `scripts/run_mvp_repro.sh`**

要求：
- 先检查关键输入是否存在（SelfCap 数据、4DGV 数据、脚本是否存在）
- 提供最小可复现路径（不追求完整对比）：
  - Gate-0 真实 smoke（若数据存在）
  - SelfCap adapter（若脚本与 tar 存在）
  - 最后调用 `python3 scripts/build_report_pack.py`

Run:
```bash
cd /root/projects/4d-recon
bash scripts/run_mvp_repro.sh --help | head
```

Expected:
- `--help` 输出包含至少 1 个可选参数（例如 `--gpu` 或 `--max-steps`）。

**Step 4: 创建证据包文档骨架（先占位，后续填结果）**

Run:
```bash
cd /root/projects/4d-recon
cat > outputs/report_pack/ablation_notes.md <<'MD'
# Ablation Notes

## baseline vs zero_velocity (T0)
- Dataset:
- Key settings:
- Outcome:

## Gate-0 real smoke (4DGV_DeskGames)
- Dataset:
- Key settings:
- Outcome:
MD

cat > outputs/report_pack/failure_cases.md <<'MD'
# Failure Cases (Mechanism-Level)

1. Scene scale / max_dist filtering can drop all Gaussians (fix via --global-scale).
2. render_traj_path=arc may hit singular matrix for degenerate camera layouts (use fixed).
3. Per-frame sparse with non-contiguous frame ids needs remap; otherwise train range mismatch.
MD

cat > notes/demo-runbook.md <<'MD'
# Demo Runbook

## Commands
- Gate-0 real smoke:
- Gate-1 SelfCap smoke:
- T0 audit:

## Playback files
- MP4 paths:

## Fallback
- If Gaussians become 0:
- If singular matrix:
MD

cat > notes/qna.md <<'MD'
# Q&A

## What is the data contract?

## Why do we need an adapter?

## What does T0 prove?
MD
```

Expected:
- 四个 md 文件都存在，且可打开。

---

### Task C2: T0 审计（优先 SelfCap 真动态；否则用 fixture 兜底）

**Files:**
- Create: `projects/4d-recon/notes/t0-gate-decision.md`
- Modify: `projects/4d-recon/scripts/t0_grad_check.md`
- Create: `projects/4d-recon/outputs/t0_selfcap/*`（运行时生成）

**Step 1: 等待/确认 SelfCap Gate-1 数据已就位（B 的产物）**

Run:
```bash
cd /root/projects/4d-recon
test -d data/selfcap_bar_8cam60f/triangulation && echo "SelfCap dataset ready" || echo "SelfCap dataset missing"
```

Expected:
- 若输出 `SelfCap dataset ready`，走 Step 2/3/4；
- 否则跳到 Step 1b（fixture 兜底），并在 B 完成后再回头重跑 SelfCap。

**Step 1b（兜底）: 软链到 worktree 的 Gate-1 fixture（确保 velocity 非零）**

Run:
```bash
cd /root/projects/4d-recon
mkdir -p data
if [ ! -e data/gate1_fixture_adapted_v2 ]; then
  ln -s "$(realpath .worktrees/mvp-recovery-20260223/data/gate1_fixture_adapted_v2)" data/gate1_fixture_adapted_v2
fi
```

Expected:
- `test -d data/gate1_fixture_adapted_v2/triangulation` 返回码为 0。

**Step 2: combine_frames 生成 init NPZ（选 SelfCap 或 fixture 其中之一）**

选择其一：
- SelfCap：`DATA_DIR=data/selfcap_bar_8cam60f`，`TOTAL_FRAMES=60`
- Fixture：`DATA_DIR=data/gate1_fixture_adapted_v2`，`TOTAL_FRAMES=12`

Run（示例：SelfCap 60 帧）:
```bash
cd /root/projects/4d-recon
source third_party/FreeTimeGsVanilla/.venv/bin/activate
mkdir -p outputs/t0_selfcap
python third_party/FreeTimeGsVanilla/src/combine_frames_fast_keyframes.py \
  --input-dir data/selfcap_bar_8cam60f/triangulation \
  --output-path outputs/t0_selfcap/keyframes_60frames_step5.npz \
  --frame-start 0 \
  --frame-end 59 \
  --keyframe-step 5
```

Expected:
- `outputs/t0_selfcap/keyframes_60frames_step5.npz` 存在。

**Step 3: GPU2 跑 baseline（写 t0_grad.csv + 出视频）**

Run（示例：SelfCap）:
```bash
cd /root/projects/4d-recon
source third_party/FreeTimeGsVanilla/.venv/bin/activate
CUDA_VISIBLE_DEVICES=2 python third_party/FreeTimeGsVanilla/src/simple_trainer_freetime_4d_pure_relocation.py default_keyframe_small \
  --data-dir data/selfcap_bar_8cam60f \
  --init-npz-path outputs/t0_selfcap/keyframes_60frames_step5.npz \
  --result-dir outputs/t0_selfcap/baseline \
  --start-frame 0 \
  --end-frame 60 \
  --max-steps 200 \
  --eval-steps 200 \
  --save-steps 200 \
  --render-traj-path fixed \
  --global-scale 12 \
  --t0-debug-interval 50 \
  --t0-grad-log-path outputs/t0_selfcap/baseline/t0_grad.csv
```

Expected:
- `outputs/t0_selfcap/baseline/t0_grad.csv` 存在。
- `outputs/t0_selfcap/baseline/videos/traj_4d_step199.mp4` 存在（或相邻 step 文件）。

**Step 4: GPU2 跑 zero-velocity（同样写 grad + 出视频）**

Run:
```bash
cd /root/projects/4d-recon
source third_party/FreeTimeGsVanilla/.venv/bin/activate
CUDA_VISIBLE_DEVICES=2 python third_party/FreeTimeGsVanilla/src/simple_trainer_freetime_4d_pure_relocation.py default_keyframe_small \
  --data-dir data/selfcap_bar_8cam60f \
  --init-npz-path outputs/t0_selfcap/keyframes_60frames_step5.npz \
  --result-dir outputs/t0_selfcap/zero_velocity \
  --start-frame 0 \
  --end-frame 60 \
  --max-steps 200 \
  --eval-steps 200 \
  --save-steps 200 \
  --render-traj-path fixed \
  --global-scale 12 \
  --t0-debug-interval 50 \
  --t0-grad-log-path outputs/t0_selfcap/zero_velocity/t0_grad.csv \
  --force-zero-velocity-for-t0
```

Expected:
- `outputs/t0_selfcap/zero_velocity/t0_grad.csv` 存在。
- `outputs/t0_selfcap/zero_velocity/videos/traj_4d_step199.mp4` 存在（或相邻 step 文件）。

**Step 5: 自动判定（finite + 非全 0）并填入文档**

Run:
```bash
cd /root/projects/4d-recon
python3 - <<'PY'
import csv, math
from pathlib import Path
\ndef check(p: Path):\n    rows=list(csv.DictReader(p.open()))\n    gv=[float(r.get('grad_v_norm',0) or 0) for r in rows]\n    gd=[float(r.get('grad_duration_norm',0) or 0) for r in rows]\n    finite=all(math.isfinite(x) for x in gv+gd)\n    nonzero_v=sum(1 for x in gv if abs(x)>0)\n    nonzero_d=sum(1 for x in gd if abs(x)>0)\n    return len(rows), finite, nonzero_v, nonzero_d\n\nfor name in ['baseline','zero_velocity']:\n    p=Path('outputs/t0_selfcap')/name/'t0_grad.csv'\n    if not p.exists():\n        print(name,'MISSING',p)\n        continue\n    rows, finite, nzv, nzd=check(p)\n    print(name,'rows',rows,'finite',finite,'nonzero_v',nzv,'nonzero_d',nzd)\nPY
```

Expected:
- baseline 与 zero_velocity 都满足：`finite True` 且 `nonzero_v/nonzero_d > 0`（至少其中之一非 0）。

**Step 6: 写 `notes/t0-gate-decision.md` + 更新 `scripts/t0_grad_check.md`**

写入：
- 绝对时间戳
- 数据集路径（SelfCap 或 fixture）
- 两个 MP4 路径 + 两个 CSV 路径
- 结论：PASS/FAIL（按 Step 5 输出）

---

### Task C3: 生成离线证据包（断网可展示）

**Files:**
- Create: `projects/4d-recon/outputs/report_pack_2026-02-25.tar.gz`

**Step 1: 打包**

Run:
```bash
cd /root/projects/4d-recon
python3 scripts/build_report_pack.py
shopt -s nullglob
tar -czf outputs/report_pack_2026-02-25.tar.gz outputs/report_pack notes outputs/t0_* outputs/gate0_* outputs/gate1_*
ls -lh outputs/report_pack_2026-02-25.tar.gz
```

Expected:
- `outputs/report_pack_2026-02-25.tar.gz` 存在且大小 > 0。
