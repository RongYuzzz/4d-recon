# Owner A (GPU0) Implementation Plan

> 状态：已完成（存档）。不要再按本文执行；当前入口以 `README.md` / `notes/demo-runbook.md` 为准。

**Goal:** 在 `projects/4d-recon`（主阵地）固化“入口脚本 + 训练透传 + 真实数据 Gate-0 smoke”，并用 GPU0 复跑产出 1 个可播放视频与对应 `stats`，作为可复现盖章证据。

**Architecture:** 从 `.worktrees/mvp-recovery-20260223` 抽取已验证的 adapter/gate 脚本与 `run_pipeline.sh` 透传能力，同步回 `projects/4d-recon`。数据层先用软链复用 worktree 已下载的 `turandai/4DGV_DeskGames` 样例，避免重复下载与路径不一致。最后在 GPU0 跑一次短 smoke，确认 `outputs/` 产物在主阵地可复现。

**Tech Stack:** Bash, Python, PyTorch, COLMAP, FreeTimeGsVanilla

---

### Task A1: 同步 worktree 的入口脚本到主阵地

**Files:**
- Create: `projects/4d-recon/scripts/adapt_hf_sample_to_freetime.py`
- Create: `projects/4d-recon/scripts/run_gate0_smoke.sh`
- Create: `projects/4d-recon/scripts/run_gate1_smoke.sh`
- Create: `projects/4d-recon/scripts/generate_synthetic_scene01.py`
- Modify: `projects/4d-recon/README.md`

**Step 1: 复制脚本文件（只拷贝代码，不拷贝 data/outputs）**

Run:
```bash
cd /root/projects/4d-recon
cp -a .worktrees/mvp-recovery-20260223/scripts/adapt_hf_sample_to_freetime.py scripts/
cp -a .worktrees/mvp-recovery-20260223/scripts/run_gate0_smoke.sh scripts/
cp -a .worktrees/mvp-recovery-20260223/scripts/run_gate1_smoke.sh scripts/
cp -a .worktrees/mvp-recovery-20260223/scripts/generate_synthetic_scene01.py scripts/
chmod +x scripts/run_gate0_smoke.sh scripts/run_gate1_smoke.sh scripts/*.py
```

Expected:
- `ls -la scripts/run_gate0_smoke.sh` 显示可执行位。

**Step 2: 更新主阵地 README（让别人不看 worktree 也能跑）**

Edit: `projects/4d-recon/README.md`
- 加入两条入口命令：
  - Gate-0：`bash scripts/run_gate0_smoke.sh ...`
  - Gate-1：`bash scripts/run_gate1_smoke.sh ...`
- 说明需要的环境变量：
  - `MAX_STEPS`、`RENDER_TRAJ_PATH`、`RENDER_TRAJ_TIME_FRAMES`、`EXTRA_TRAIN_ARGS`

Expected:
- `rg -n "run_gate0_smoke|run_gate1_smoke|EXTRA_TRAIN_ARGS" README.md` 有命中。

---

### Task A2: 同步训练入口的环境变量透传（避免手改 trainer）

**Files:**
- Modify: `projects/4d-recon/third_party/FreeTimeGsVanilla/run_pipeline.sh`

**Step 1: 用 worktree 版本覆盖（最快，减少漏改）**

Run:
```bash
cd /root/projects/4d-recon
cp -a .worktrees/mvp-recovery-20260223/third_party/FreeTimeGsVanilla/run_pipeline.sh third_party/FreeTimeGsVanilla/run_pipeline.sh
chmod +x third_party/FreeTimeGsVanilla/run_pipeline.sh
```

Expected:
- `rg -n "MAX_STEPS|EVAL_STEPS|SAVE_STEPS|RENDER_TRAJ_PATH|RENDER_TRAJ_TIME_FRAMES|EXTRA_TRAIN_ARGS" third_party/FreeTimeGsVanilla/run_pipeline.sh` 至少命中一次。

---

### Task A3: 同步关键测试（先保证脚本不回退）

**Files:**
- Create: `projects/4d-recon/scripts/tests/test_adapt_hf_sample.py`
- Create: `projects/4d-recon/scripts/tests/test_adapt_hf_sample_per_frame_sparse.py`
- Create: `projects/4d-recon/scripts/tests/test_run_gate1_smoke_frame_count.py`
- Create: `projects/4d-recon/scripts/tests/test_run_pipeline_env_flags.py`
- Create: `projects/4d-recon/scripts/tests/test_run_pipeline_extra_train_args.py`

**Step 1: 复制测试文件**

Run:
```bash
cd /root/projects/4d-recon
mkdir -p scripts/tests
cp -a .worktrees/mvp-recovery-20260223/scripts/tests/test_adapt_hf_sample.py scripts/tests/
cp -a .worktrees/mvp-recovery-20260223/scripts/tests/test_adapt_hf_sample_per_frame_sparse.py scripts/tests/
cp -a .worktrees/mvp-recovery-20260223/scripts/tests/test_run_gate1_smoke_frame_count.py scripts/tests/
cp -a .worktrees/mvp-recovery-20260223/scripts/tests/test_run_pipeline_env_flags.py scripts/tests/
cp -a .worktrees/mvp-recovery-20260223/scripts/tests/test_run_pipeline_extra_train_args.py scripts/tests/
```

Expected:
- `ls -la scripts/tests` 能看到以上 5 个文件。

**Step 2: 跑脚本测试（只跑同步相关最小集合）**

Run:
```bash
cd /root/projects/4d-recon
PY=/root/projects/4d-recon/third_party/FreeTimeGsVanilla/.venv/bin/python
$PY scripts/tests/test_export_triangulation_adapter.py
$PY scripts/tests/test_run_pipeline_env_flags.py
$PY scripts/tests/test_run_pipeline_extra_train_args.py
$PY scripts/tests/test_adapt_hf_sample.py
$PY scripts/tests/test_adapt_hf_sample_per_frame_sparse.py
$PY scripts/tests/test_run_gate1_smoke_frame_count.py
```

Expected:
- 全部 `PASS`。

---

### Task A4: 主阵地数据入口（最省时：先 symlink 复用 worktree 已下载真实数据）

**Files:**
- Create (symlink): `projects/4d-recon/data/hf_4dgv_cook8_source`

**Step 1: 建立数据软链**

Run:
```bash
cd /root/projects/4d-recon
mkdir -p data
if [ ! -e data/hf_4dgv_cook8_source ]; then
  ln -s "$(realpath .worktrees/mvp-recovery-20260223/data/hf_4dgv_cook8_source)" data/hf_4dgv_cook8_source
fi
test -d data/hf_4dgv_cook8_source/sparse/0
```

Expected:
- 最后一行命令返回码为 0。

---

### Task A5: GPU0 盖章复跑（Gate-0 真实 smoke）

**Files:**
- Create: `projects/4d-recon/outputs/gate0_4dgv_cook8_gs6/*`（运行时生成）

**Step 1: 在 GPU0 跑 40 steps（足够出视频 + stats）**

Run:
```bash
cd /root/projects/4d-recon
EXTRA_TRAIN_ARGS='--global-scale 6' \
MAX_STEPS=40 \
RENDER_TRAJ_PATH=fixed \
bash scripts/run_gate0_smoke.sh \
  data/hf_4dgv_cook8_source \
  data/gate0_4dgv_cook8_gs6 \
  outputs/gate0_4dgv_cook8_gs6 \
  0 24 5 default_keyframe_small
```

Expected:
- `outputs/gate0_4dgv_cook8_gs6/videos/traj_4d_step39.mp4` 存在。
- `outputs/gate0_4dgv_cook8_gs6/stats/val_step0039.json` 存在。

**Step 2: 快速验证 stats 可读**

Run:
```bash
cd /root/projects/4d-recon
python3 - <<'PY'
import json
from pathlib import Path
p=Path("outputs/gate0_4dgv_cook8_gs6/stats/val_step0039.json")
obj=json.loads(p.read_text())
print("PSNR", obj.get("psnr"), "SSIM", obj.get("ssim"), "LPIPS", obj.get("lpips"))
PY
```

Expected:
- 打印出 `PSNR/SSIM/LPIPS` 三个数值（不要求好看，但必须是数）。
