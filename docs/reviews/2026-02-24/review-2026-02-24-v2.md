拍板：**选 A（VGGT feature metric loss）作为 02-26+ 的唯一主线**。
 B（triangulation→3D velocity 初始化）只作为 **Plan‑B 的救火开关**；C（并行两条）在你们当前“可复现但研究收益不稳”的状态下，会把 GPU 预算和注意力都撕裂，最后两条都做成半成品。

这个结论不是“喜好”，是被你们现有证据强行推出来的：**control_weak_nocue_600 > ours_weak_600**，说明“mask 级 cue + photometric reweight”目前要么无效，要么在拖后腿；继续在 KLT strong v2 上堆工程，只会在答辩时被打成“题文不符（大模型先验悬空）”。你们自己的 Progress 也已经把 strong 线按 stoploss 暂停大投入、把瓶颈定位到 cue mining 的质量/收益上了。Progress
 而执行计划最开始的主叙事就是“VGGT 线索挖掘 + 注意力/一致性约束挂载到 4DGS”，现在最缺的是把 VGGT 从“mask 工具人”升级为“可辩护的 prior”。2026-02-12-4d-reconstruction-ex…
 同行的修正方案里，真正最值钱、最短路径的也是 feature metric loss。Evaluation_and_Correction_Plan

下面是“怎么改计划”的**具体落地版本**（你可以直接写进 `docs/execution/...` 的 02-26+ 章节，替换掉原 B 主攻方向）。2026-02-12-4d-reconstruction-ex…

------

## 1) 02-25 不大改：只做口径微调（你们已对齐）

你同伴的结论对：02-25 不需要大改，只要**口径微调**：

- **KLT**：明确降级为 `baseline/attempt/bridge`，放在“验证 strong 接口连通性”的位置，不再占“创新点”。（这和你们 Progress 的 strong attempt + stoploss 一致）Progress
- **Weak**：把“无稳定收益/甚至 control 更好”写成“关键发现”，并引出“因此必须把 VGGT prior 提升到 feature level”。

这一块你们按现有 evidence 链就能讲通，不动协议 v1。Progress

------

## 2) 02-26+ 必须大改：把 B 主线从 “KLT strong v2” 换成 “VGGT feature metric loss”

### 为什么 A 是唯一主线（硬理由）

1. **叙事闭环**：feature loss 是“VGGT 先验进入优化闭环”的最短路径；mask reweight 只能算 loss reweight，很容易被评审说成“工程 trick”。
2. **证据支持要改**：你们自己的结果里，control（无 cue）最好，说明 cue/注入方式有问题；继续围绕 cue 做强融合（尤其 KLT）会越辩越虚。Progress
3. **工程可控**：feature loss 可以做成“低频、低分辨率、patch 采样、离线缓存 GT 特征”，不会把 pipeline 拖死；反而 2D flow→3D scene flow（B 原提法）对 depth 依赖强，很容易陷入“自洽幻觉”和算力黑洞（你同伴也已经指出）。Evaluation_and_Correction_Plan

------

## 3) 新主线 A 的“可执行规格”（你们要写进计划的那种）

### A‑Mainline：VGGT Feature Metric Loss（Weak v2 / Strong 可控版）

#### A0. 先补两页防守证据（不讨论，直接加）

这两项是“答辩防守必需品”，你们同伴也要求加：

- **Velocity 统计图**：step0/100/600 的 `||v||` 分布 + 动态区域内 `||v||` 分布（动态区域可以先用简单帧差/你们已有 cue 近似）。
- **Cue 对齐 sanity**：mask overlay（每个 cam、每帧抽样几张），明确坐标系/resize/值域方向都正确。

这一步的意义：把“zero velocity 死路”的攻击彻底关门——即便批评点打偏了，你也用统计图封死对方嘴。你们 Progress 已经把 T0 与协议冻结当作工程优势，这两页图就是把优势变成“可展示证据”。

#### A1. 离线预计算 GT 特征（必须离线，否则你训练速度直接腰斩）

- 对 **GT 图像**：`VGGT(I_gt)` 在固定分辨率（建议 downscale 后，比如 1/4）离线算好，存成：
  - `features/{cam}/{frame}.pt` 或 `.npz`
  - 同时存 meta：输入归一化方式、层名、stride、H/W
- 这一步是“training-free cue mining”的延伸：VGGT 冻结，仅 forward。2026-02-12-4d-reconstruction-ex…

#### A2. 训练时只对 render 算特征（低频 + patch）

在训练 loop 里加一项：
$$
L_{feat} = \|\phi(I_{render}) - \phi(I_{gt})\|_2
$$
其中 $\phi$ 是 VGGT 的某一层/多层特征。Evaluation_and_Correction_Plan

**工程约束（写进 stoploss/验收里）：**

- **低频触发**：每 N step 才算一次（N=4 或 8 起步）
- **低分辨率**：render 送 VGGT 前先 resize 到固定小分辨率
- **patch 采样**：只取 K 个 patch（比如 K=32），而不是整张 feature map 全算
- **可选 gating**：只在 “疑似动态区域” patch 上触发（可以先用帧差，不必依赖不稳定 cue）

#### A3. 权重调度（不然你会直接把几何压死）

- 前 100 steps（或前 20% steps）**不启用** feature loss（warmup）
- 然后线性 ramp 到目标权重 λ_feat
- λ_feat 的 sweep 必须极小范围（比如 {0.01, 0.05, 0.1}），不要大范围乱扫

#### A4. 你们应该怎么定义“成功”

沿用你们 protocol v1 的指标体系（PSNR/SSIM/LPIPS/tLPIPS），并补一条硬线：Progress

- **最低成功线（任何一条满足即可）**：
  - tLPIPS 下降 ≥ 10%（主打“时序稳定性”叙事）
  - 或 LPIPS 下降 ≥ 0.01
  - 或 PSNR +0.2dB
- **失败线（触发止损）**：
  - 训练明显不稳（loss 爆、render 闪烁更严重）
  - 或计算成本不可接受（训练吞吐下降 >2× 且无法通过 N/patch 降下来）
  - 或 2 次 full run 都没有任何正向趋势

#### A5. 强制消融（让你“工作量看起来像研究”，不是调参）

至少跑这 3 个（都在同一 protocol v1 下）：Progress

1. baseline（已有）
2. control_weak_nocue（已有，关键对照）
3. feature_loss_v1（新主线）

再加一个极低成本消融：

4) feature_loss_v1 + dynamic‑patch gating（用帧差 gating 或 cue gating）

------

## 4) B 计划怎么放：不是主线，但给你留“救火开关”

你同伴问的 B（triangulation→3D velocity 初始化）我建议这样定位：

- **不作为唯一主线**，原因：你们现在并没有证据证明“速度信号缺失”是根因；相反，你同伴已经抽样算过 keyframes npz 里 `||v||` 统计并非全零（这能写进“反驳 zero‑velocity 误判”的证据页）。
- **作为 Plan‑B**：当且仅当 A‑Mainline 证明“feature prior 引入后仍然没有任何趋势”，才启动 B，并且必须满足两个条件：
  1. 不做 2D flow→3D scene flow 的 depth lift（那条最容易拖死）Evaluation_and_Correction_Plan
  2. **优先利用你们已有的 per‑frame triangulation** 做 3D 点云帧间粗匹配/差分来写 velocity（闭环一致、最符合你们数据契约优势）Progress

B 的 timebox：**48h**，只允许做成“初始化脚本 + 一次 full run 对比”，不允许演变成新项目。

------

## 5) C 计划（scoreboard/evidence）不改，但要加两条“强制记录”

你们 Progress 已经把 scoreboard/evidence 链路当刚需，这是对的。Progress
 微调点只有两个：

- scoreboard 必须新增条目：`feature_loss_v1`、`feature_loss_v1_gated`
- scoreboard 里必须显式标红：`control > ours_weak`（风险提示 + 行动项），否则你们会下意识回避这个刺眼事实

------

## 6) 你问“要不要并行 C（两条并行各 48–72h）？”——我的答案：**不建议**

并行的前提是“两条路线都低耦合、都能在一周内收敛出结论”。你们现在不是这个状态：

- 你们当前的瓶颈已经明确是 **cue/注入方式的有效性**，强行并行 velocity init 只会把精力从“VGGT prior 上桌”分走。Progress
- feature loss 是更“论文级”的主线；velocity init 是“工程级”的辅助。主线没立住就去搞辅助，只会让你答辩时看起来像在补漏洞。

所以：**A 单线推进 + B 作为触发式 Plan‑B**，是你们最像“导师会签字”的推进方式。

------

# 最终拍板（可以原样写进纪要）

- **02-25**：协议 v1/evidence 链不大改；KLT 降级为 baseline/attempt；weak 无稳定收益写为关键发现。
- **02-26+ 唯一主线**：**A) VGGT feature metric loss（离线 GT feature、训练时低频/低分辨率/patch）**。
- **B 计划**：不并行；仅作为 Plan‑B（触发式 48h timebox），优先用 triangulation 做 3D velocity 粗初始化，不做 depth‑lift scene flow。
- **新增诊断任务**：v 分布统计页 + cue 对齐 sanity overlay（写进后续计划与 evidence pack）。

如果你要我再“更落地一层”：我可以把 **A‑Mainline 的训练循环接口**拆成你们 repo 里应该新增/修改的文件清单（例如 `scripts/precompute_vggt_features.py`、`losses/vggt_feature_loss.py`、`configs/feature_loss_v1.yaml`、scoreboard 扩展项），并给出每个文件的最小实现规格与验收点，保证你们不会在工程细节里走散。