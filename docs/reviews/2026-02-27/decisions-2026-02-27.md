我先把结论拍板：**后续修正不走“硬拽回 VGGT 单主线”，而是把当前已经跑通且证据很硬的 Plan‑B 纳入开题路线，重构为“物理运动先验（Plan‑B）+ 几何语义先验（VGGT）”的双阶段框架**。这样既能最大化复用你们现有成果，又能把开题里承诺的 VGGT/动静解耦/时空一致性“补齐到能答辩”的程度。

下面我分两条线：**(I) 开题文档怎么改（叙事对齐）**、**(II) 接下来研发怎么做（技术对齐）**，每条都给“能直接落到仓库 TODO/决议文件”的版本。

------

## 1) 你们现在与开题路线的差异，核心不是“小偏差”，而是主叙事已切换

### 1.1 开题报告承诺的技术路线（你写得非常明确）

开题的三条研究主线是：

1. **VGGT 潜在实例语义/动态线索挖掘**（不依赖 2D 分割/跟踪器，谱聚类得到伪掩码等）
2. **VGGT 全局注意力 → 长程时空关联**，并设计“注意力引导的对比损失”，把跨帧对应融入 4DGS 优化
3. **动静显式解耦 + 可编辑性验证（物体移除）**，并计划在 Neural3DV/Multi‑Human 做定量与 mIoU 等评测

### 1.2 当前进度/主线（从你们冻结决议与会议包口径看）

你们现在的“主线与证据链”已经收口为 **Plan‑B（速度初始化）**，并进入写作冲刺，预算当时是 **新增 full600=0**；VGGT/weak/feature‑loss 等路线被收束为“尝试/负结果/止损”。

### 1.3 这会导致的最大风险（同行 A 说得很准）

如果不修正，答辩/评审会出现最致命的口径攻击：

> “你开题说要做 VGGT 语义/注意力驱动的 4D 重建，最后交付的是速度初始化改进。你到底研究了什么？”

所以你需要的不是“写作时解释一下”，而是**把路线与承诺重新对齐**：要么改开题，要么补齐技术模块（至少补齐到“可实现 + 可复现 + 有证据”的程度）。

------

## 2) 我给的最终拍板：采用“双重引导框架”，同时做“文档修订 + 研发补齐”

同行 A 给了两条路：A=对齐仓库现状、B=坚持原开题；同行 B 给了折中但更强的第三条路：**把二者合并成双阶段**。我建议你直接采用同行 B 的“新叙事”，但要按同行 A 的“可落地要求”把细节补齐。

### 2.1 新主线一句话（以后所有文档都统一）

> **先用 Plan‑B 提供物理合理的初始运动（打破零速/劣质速度基底的收敛陷阱），再用 VGGT 的几何一致语义特征提供高维度时空对齐约束（Feature Metric / Attention‑guided Consistency），最终在动静解耦渲染中展示可编辑性（object removal）。**

这样做的好处：

- **Plan‑B 不再是“偏题产物”，而是开题路线的“阶段一（物理底座）”**。
- 你仍然兑现开题核心：VGGT cue/attention + 动静解耦 + 编辑演示，只是把它们放到“已修复速度基底之后”来做，避免重蹈 feature-loss v2 的失败模式。

------

## 3) 开题报告怎么改：给你一份“必须改的清单 + 推荐替换话术”

下面这部分你可以直接照着改 `4D-Reconstruction.md`（或者新建 `4D-Reconstruction-v2.md`，保留历史版本）。

### 3.1 必须新增：贡献列表（解决“新在哪”）

同行 A 明确指出你现在缺“对标与差异点”，评审会直接问你相对 VGGT4D/Split4D/FreeTimeGS 新在哪。

我建议你新增一个 **【本文主要贡献】**段，结构按同行 B 的三条对标写（可直接用）：

- 对比 FreeTimeGS：我们识别并修复“零速/劣速基底”的收敛陷阱，引入显式 3D 物理运动先验（Plan‑B velocity init），显著改善短预算下的时序稳定性（tLPIPS）。
- 对比 VGGT4D：我们不把大模型当“2D 掩码生成器”，而是把 VGGT 的几何一致特征用于 4DGS 的特征空间一致性约束（feature metric / attention‑guided consistency）。
- 对比 Split4D：我们避免过重的逐帧对比学习管线，采用更轻量的 render‑and‑compare 特征一致性约束，并提供可审计的证据链与止损机制。

> 关键点：贡献必须是“可验证条目”，不要写愿景句。

### 3.2 必须改：把“端到端”表述改成可辩护版本

同行 A 已经精准指出矛盾：你说不依赖外部 2D 分割/跟踪器，但又有“VGGT→谱聚类→伪掩码”的外置流程；这会被抓“你这不 end‑to‑end”。

推荐替换话术（你直接全局替换）：

- 原：不依赖任何外部 2D 分割或跟踪器作为前置模块
- 改：**无需外部 2D 强监督前置（如 SAM/DEVA 掩码或 tracker 轨迹）**；VGGT 提供的是冻结的几何语义软先验（soft prior），可以在训练前或训练中生成并以 loss/weight 形式注入。

### 3.3 必须补：把“注意力→对应矩阵→对比损失”写成可实现的版本

同行 A 的攻击点你必须正面解决：像素级全连接对应是 (O((HW)^2))，会被认为“概念正确但不可实现”。

你需要在开题里写清楚至少一种可落地稀疏策略（推荐你写 **patch‑level + top‑k**，最稳）：

- **Patch/token 级**而非像素级（把 (H\times W) 降到 (N) 个 token）
- 对每个 token 只取 **top‑k** 对应（k=1~8），形成稀疏集合 (\mathcal{M})
- loss 只在 (\mathcal{M}) 上计算（而不是全连接矩阵）

此外还要补齐“loss 落点”三问（同行 A 点名了）：高斯特征是什么、像素↔高斯怎么绑定、正负样本怎么构造。

> 你不需要在开题写到论文级推导，但至少要给“定义 + 伪代码级描述”，否则会被判“不可复现”。

### 3.4 必须改：评测口径从“mIoU 必优于”改成“对标/力争”，并与 SelfCap+tLPIPS 对齐

同行 A 说得很清楚：你现在写“mIoU 优于依赖外部先验的方法”，会被追问 GT 从哪来、你凭什么必胜；而同行 B 进一步建议直接删除 Neural3DV/Multi‑Human + mIoU 的不切实际承诺，改成 SelfCap + tLPIPS + 视觉去闪烁。

推荐你把预期目标改成这种“可答辩格式”：

- **主指标**：PSNR/SSIM/LPIPS + tLPIPS（时序稳定）
- **数据集**：以 SelfCap 为主（符合你们当前管线），Neural3DV/Multi‑Human 作为“后续扩展/时间允许再做”
- **措辞**：从“优于”改为“对标/力争达到/在特定预算下显著改善”

### 3.5 必须补：资源约束与止损策略（把 Plan‑B 写进开题）

同行 A 指出你开题排期理想化、缺算力预算/止损；同行 B 明确建议：把 Plan‑B 写成保底交付与止损回退。

你应该新增小节：

- 单次 full run 的预算（至少写“600 steps/多少小时/占用几卡”）
- 模块分级：MVP 必做（Plan‑B + 基础渲染 + tLPIPS）；加分项（VGGT feature metric / attention guided）；高风险项（真正长程对应/对比学习）
- 止损线：比如“48–72h 没看到趋势就回退到上一档”

------

## 4) 接下来研发怎么做，才能“技术上对齐开题”而不是只改文档

你说现在时间不紧张——那就把开题里承诺的三块补齐成**能拿出证据的最小实现**。我按“对齐收益最大/风险最低”的顺序给你路线图（每条都对应开题三主线）。

### 任务 A：兑现“动静解耦 + 可编辑性”（最稳、最容易做出强定性结果）

开题明确承诺“动静显式分离 + 物体移除/编辑验证”。

**拍板：必须做，而且可以用你们现有 Plan‑B 直接做。**

最低成本实现（同行 B 给了很好的可落地方案）：

1. 训练完成后，为每个 Gaussian 计算平均速度幅值 (||v||)
2. 用阈值 (\tau) 分成 static/dynamic（例如 (||v||<\tau) 为静态）
3. 渲染时只送入静态高斯 → 得到“移除所有动态物体后的背景视频”
4. 反过来只渲染动态高斯 → 得到动态层

**交付物（写进论文/答辩）**：

- 一段“背景干净视频”（强视觉冲击）
- 一张速度直方图 + 阈值说明
- 至少 1 个失败例（比如慢动的人被判成静态）作为 limitation

这块是“开题目标 3”的最强对齐点，几乎不需要再烧大量新训练。

------

### 任务 B：补齐“VGGT 线索挖掘”（先做可视化与伪掩码，别一上来就追闭环收益）

开题第一块是：从 VGGT 内部层挖实例级线索，做聚类得到伪掩码。

**拍板：先把它做成“可解释材料”，再尝试作为监督。**
原因：你们历史上“cue 注入可能负增益”的风险信号已经出现过；现在要对齐开题，最安全的路径是先把“VGGT 确实挖到了什么”证明出来（图像/视频），再谈“它能不能提指标”。

推荐最小实现：

- 抽 3–5 帧 × 多视角，取 VGGT 某一层的 feature map
- 对 feature 做 PCA/TSNE 可视化，或对像素/patch 做 k‑means/spectral clustering 得到 K 个簇
- 输出：彩色簇图 + 跨视角对应一致性的对比图（这就是你开题写的“几何一致语义线索”证据）

只要你能给出“VGGT 的确提供了跨视角更一致的语义分组”，开题第一块就算兑现一半了（并且能写进 Method/Appendix 当 Figure）。

------

### 任务 C：重启 “Plan‑B + VGGT Feature Metric Loss”（这是把你们现状对齐开题主线的关键实验）

开题第二块的核心是：把 VGGT attention/对应约束融入优化，提升时空一致性。
同行 B 的建议非常明确：Feature Loss 以前失败可能是因为“零速陷阱”，现在 Plan‑B 提供了物理运动底座，应当重启并做 warmup/降权。

**拍板：必须做，但要按“可实现版本”来做，别上来就写一个不可跑的 (O((HW)^2))。**

我建议你把开题里“注意力引导对比损失”落地为一个更稳的版本：

- 给每个 Gaussian 增加一个可学习特征 (f_i\in\mathbb{R}^d)（d=8/16）
- rasterizer 渲染出 feature map (F_{\text{render}}^t)（对高斯特征加权 splat）
- 从 VGGT 得到 feature map (\Phi^t)（冻结，不回传）
- 定义 **Feature Metric Loss（蒸馏式）**：
  [
  L_{\text{feat}}=\sum_{p\in\Omega} w(p),\big| \text{norm}(F_{\text{render}}^t(p))-\text{norm}(\Phi^t(p))\big|_2^2
  ]
  这里 (w(p)) 可以来自你在任务 B 得到的 soft mask / consistency score。

这版的好处：

- **计算可控**（不需要跨帧全连接对应）
- “像素↔高斯绑定”天然由 rasterizer 的 splatting 权重完成，回答了同行 A 的质疑点之一
- 仍然在做“VGGT 几何语义先验引导 4DGS”，与开题精神一致

然后再做一个“更贴近开题的时序项”（小步快跑）：

- 只在 **相邻帧** 做 patch‑level top‑k 对应（稀疏集合），加一个 temporal consistency 项（不需要长程、先证明短程有效）。

**训练策略（必须写进止损/协议）：**

- warmup：前 100–200 steps 不开 (L_{\text{feat}})
- (\lambda_{\text{feat}}) 从 0 线性爬到目标值（避免一上来就把优化带偏）
- 若出现“control 反而更好”的现象（你们历史上出现过这种风险信号），立刻记录并把它写成 failure analysis，而不是继续硬调参。

------

### 任务 D：真正的“注意力引导对应/对比学习”（可选加分项，严格 timebox）

这才是开题第二块的“完全体”：VGGT attention → 跨帧对应 → 对比损失。

同行 A 的意见是：你必须写清稀疏化，否则会被认为不可行。

**拍板：它是加分项，不允许拖死主线；给它 72 小时封顶的 timebox。**（这条纪律建议你写进新决议文件里）

推荐落地形态（最贴近开题、又不爆炸）：

- patch/token 级（例如 32×32 token 网格）
- 每个 token 只取 top‑k 对应（k=1~4）
- loss 只在这些对应上约束 (F_{\text{render}}^t) 与 (F_{\text{render}}^{t+1}) 或 (\Phi) 的一致性
- 对应质量必须可视化（否则评审会说你在做噪声对比学习）

------

## 5) 你接下来应当“冻结/升级”的协议与产出（避免把 v26 证据链冲掉）

因为你们之前已经有“冻结写作冲刺”的纪律与证据包口径（Plan‑B 主线、anti‑cherrypick 等），现在要做学术完善，正确做法是：

- **保留 protocol_v1 与 v26 证据链不动**（继续作为“阶段一物理底座”的最终证据）
- 新增所有“对齐开题”的实验，一律开 **protocol_v2**（或 `protocol_semantic_v1`），并写清：
  - 新增 loss、权重范围、warmup、timebox、成功线/止损线
  - 哪些结果会写进正文、哪些只进附录/未来工作

这样你就能同时满足两件事：

1. 不破坏现有写作主线与可审计证据
2. 又能补齐开题承诺，形成“阶段一/阶段二”的完整故事

------

## 6) 最终你会得到一个“对齐开题且可答辩”的交付形态（你可以照这个写摘要）

把最终交付写成三层（对应开题三主线）：

1. **物理运动先验（Plan‑B）**：解决短预算下的劣速基底收敛问题，显著改善时序稳定性（tLPIPS）。
2. **VGGT 几何语义先验（Feature Metric/Attention‑guided Consistency）**：在物理底座上进一步提升遮挡/纹理漂移场景的稳定对齐（主要卖点是稳定性与一致性，而不是一句“更高保真”）。
3. **动静解耦与可编辑性**：基于速度/语义线索实现静态背景渲染与对象移除，给出强定性演示。

------

如果你希望我把这一套“文档修订”直接落成可提交文本：我可以按上述拍板帮你把 `4D-Reconstruction.md` 改成一版 **v2**（包含贡献列表、可实现 loss 定义、评测口径、资源/止损章节），并把改动点以 diff 风格标注出来，方便你直接合入仓库。