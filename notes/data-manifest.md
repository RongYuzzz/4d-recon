# Data Manifest

Last update: `2026-02-24`

## Canonical Inputs (Current)

### SelfCap release tarball
- HF dataset: `zju3dv/SelfCap-Dataset`
- Canonical path (scripts default): `data/selfcap/bar-release.tar.gz`
- Raw cache (recommended): `data/raw/selfcap/bar-release.tar.gz`

Notes:
- 建议让 `data/selfcap/bar-release.tar.gz` 指向 `data/raw/selfcap/bar-release.tar.gz`（避免引用 `.worktrees/...` 这种脆弱路径）。
- `scripts/adapt_selfcap_release_to_freetime.py` 直接读取 tarball，不依赖系统 `ffmpeg/ffprobe`。

### Canonical adapted dataset (Gate-1 + T0 recommended)
- Name: `selfcap_bar_8cam60f`
- Path: `data/selfcap_bar_8cam60f`
- Generated by: `scripts/adapt_selfcap_release_to_freetime.py`
- Params (recommended default):
  - `--camera_ids 02,03,04,05,06,07,08,09`
  - `--frame_start 0 --num_frames 60 --image_downscale 2`

Expected contents:
- `images/`: 8 camera dirs, each 60 frames (total 480 images)
- `sparse/0/`: `cameras.bin`, `images.bin`, `points3D.bin`
- `triangulation/`:
  - `points3d_frame*.npy`: 60
  - `colors_frame*.npy`: 60

## Legacy / Historical (Optional Cleanup)

这些目录用于早期验证或调试，当前 demo/runbook 不依赖它们；若磁盘紧张可考虑清理。

### 1) `scene_selfcap_bar_200_260` (legacy T0 run input)
- Path: `data/scene_selfcap_bar_200_260`
- Generated by: `scripts/prepare_selfcap_for_freetime.py --selfcap_root data/raw/selfcap/extracted/bar-release --out_root data/scene_selfcap_bar_200_260 --frame_start 200 --frame_end 260 --extract_images`
- Notes:
  - legacy 脚本依赖“已解压目录结构”，且 `--extract_images` 需要系统 `ffmpeg/ffprobe`。
  - 与当前 canonical 的 `data/selfcap_bar_8cam60f` 不同：它会把抽帧结果写入 `colmap/images/<cam>/` 形态。

### 2) `scene_selfcap_bar_200_260_noimg` (legacy)
- Path: `data/scene_selfcap_bar_200_260_noimg`
- Generated by: `scripts/prepare_selfcap_for_freetime.py --selfcap_root ... --out_root ... --frame_start 200 --frame_end 260`（不抽帧）

### 3) `scene_selfcap_bar_smoke` (legacy smoke)
- Path: `data/scene_selfcap_bar_smoke`
- Generated by: `scripts/prepare_selfcap_for_freetime.py --frame_start 0 --frame_end 2`

## Tooling Notes
- `ffmpeg/ffprobe`: 仅 legacy `prepare_selfcap_for_freetime.py --extract_images` 需要。
- `colmap`: 当前 canonical adapter 直接写 `sparse/0/*.bin`，不强依赖本机 `colmap` 可执行文件。

