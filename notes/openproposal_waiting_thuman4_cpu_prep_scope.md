# OpenProposal Waiting THUman4.0（CPU-only Prep）范围声明

- 本计划仅做 CPU-only 的代码与文档准备，不执行任何训练任务。
- 本计划仅写 runbook，不执行 COLMAP（feature_extractor / matcher / mapper）。
- `data/` 与 `outputs/` 下的本地评估产物禁止提交到 git。
- report-pack 禁止包含任何 GT 帧与 GT mask 原始内容（仅允许统计指标与路径引用）。

